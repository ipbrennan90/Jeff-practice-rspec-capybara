<%= form_for @event do |f| %>

<%= f.label :event_name%>
<%= f.text_field :event_name %>

<%= f.label :description %>
<%= f.text_area :description %>

<%= f.label :date %>
<%= f.date_field :date %>

<%= f.submit %>

<% end %>
